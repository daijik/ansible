---
- hosts: all
  tasks: 
  - name: tunedパッケージを最新版にする
    yum: name=tuned state=latest
  - name: util-linuxパッケージを最新版にする
    yum: name=util-linux state=latest
  - name: samba-clientパッケージを最新版にする
    yum: name=samba-client state=latest
  - name: gitパッケージを最新版にする
    yum: name=git state=latest
